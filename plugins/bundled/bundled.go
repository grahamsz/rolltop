// Package bundled is retained for compatibility with older references.
// The main server now loads runtime plugin shared objects directly from
// manifests, so this package intentionally stays empty.
package bundled
